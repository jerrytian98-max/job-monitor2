# 真实职位监测使用指南

当前版本使用 Playwright 打开真实招聘页面，不使用模拟职位。

## 推荐流程

1. 双击 `启动Web服务-用户1(默认).bat`。
2. 打开 `http://127.0.0.1:5000`。
3. 添加不带搜索关键词的招聘首页或职位列表页，确认或编辑网址标签，再填写职位关键词。
4. 填写 SMTP 发件邮箱、授权码和接收邮箱。
5. 点击“保存配置”。
6. 先点击“测试检查”，确认能找到职位。该操作不发邮件、不写数据库。
7. 点击“测试邮件发送”，确认 SMTP 可用。
8. 点击“启动监测”。

“启动监测”会先自动保存页面上的全部配置，再按列表顺序逐个抓取所有目标网址；其中一个网址失败时，程序会继续抓取后续网址。

## 招聘网址

建议填写目标公司的招聘首页或职位列表页，不需要在网址中预先填写搜索关键词。程序会自行查找岗位搜索框，把“职位关键词”逐个输入并分别搜索；遇到多个关键词时，每个关键词都会完整执行，不会因为前一个关键词已抓满而跳过后续关键词。

抓取器会优先读取招聘页面自身返回的职位数据接口，保留职位名称、地点、发布日期、岗位职责、任职要求和可直接打开的职位详情链接；遇到加密接口时会改为读取浏览器已经渲染出的完整职位卡片。列表有“下一页”时会继续读取，每个网址的每个关键词每轮最多保留 50 个职位。

当前已针对以下官方招聘平台做了专门适配：

- 腾讯招聘
- 字节跳动招聘
- 饿了么、阿里云、夸克使用的阿里招聘平台
- 小红书招聘
- Moka 招聘页（包括月之暗面、智谱等）
- 飞书招聘门户
- 美团招聘
- 拼多多集团招聘

未列出的招聘网站仍会自动寻找搜索框，并使用渲染页面中的职位详情链接作为通用回退。

“目标招聘网址”建议填写成以下形式：

```text
https://zhaopin.meituan.com/web/social
https://jobs.bytedance.com/experienced/position/list?keywords=&category=&location=&project=&type=&job_hot_flag=&current=1&limit=10&functionCategory=&tag=
https://talent.quark.cn/off-campus/position-list?lang=zh
```

网址标签显示在网址左侧的彩色圆角矩形中，可以直接编辑；标签前两个字相同时会自动使用相同颜色。不再需要的网址可以直接删除。保存配置后，网址及其标签会一起保留。

每条抓取结果的最终标签由“招聘网址标签 + 对应搜索关键词”组成。例如网址标签为“美团”，关键词分别为“法务”和“合规”，职位会分别显示“美团法务”和“美团合规”。旧版本中已经保存的带关键词网址仍然兼容，且不会重复追加同一个关键词。

每条网址右侧的绿色“+”会在下一行复制一条完全相同的网址和标签，红色“×”用于删除该行。

网页“工作日志”保持简洁，每个网址只显示“正在抓取【网址标签】”和“抓取完毕【网址标签】找到 n 个新职位”。

## 筛选顺序

职位依次经过：

1. 城市限制
2. 排除关键词
3. 职位关键词
4. 可选 Gemini 筛选
5. SQLite 去重和通知状态检查

Gemini API Key 与“AI 筛选要求”必须同时填写才会启用。AI 服务不可用时，程序会保留普通规则筛选结果，不会让整个检查失败。

## 查看已抓取职位

“爬取的职位信息”按发布日期从新到旧排列。每张卡片默认折叠，只显示网址标签、精简后的职位名称和右侧日期；点击摘要行可以展开或再次折叠，展开后显示公司、地点、薪资、来源、发布时间、发现时间、完整描述和职位链接。列表底部会显示全部页码，可直接跳转到任意一页。

“按网址标签筛选”输入框支持包含匹配。例如输入“法务”，会显示网址标签中包含“法务”的所有职位；清空输入框会恢复全部职位。

## 数据文件

- 默认用户：`config.yaml`、`jobs.db`、`jobs_cache.json`、`app.log`
- 第二用户：`config_user2.yaml`、`jobs_user2.db`、`jobs_cache_user2.json`、`app_user2.log`

只有发现新职位时才会发送招聘通知邮件。本轮所有网址的新职位会汇总到一封邮件中，产生新职位的所有网址标签都会写入标题，例如“【字节战略、字节法律】发现了 5 个新职位”；所有网址都没有新职位时不发送邮件。邮件失败的职位会保留为未通知，下次检查继续尝试；邮件成功后不会重复发送。清空职位记录会同时重置 JSON 兼容缓存。

邮件正文使用与网页“爬取的职位信息”相近的静态卡片样式，按照网页相同的日期降序展示最近 50 条以内的岗位信息，并直接显示网址标签、职位名称、日期、公司、地点、薪资、来源、发布时间、发现时间和职位描述。本轮查询新发现的岗位会显示“新”标签；职位名称是超链接，点击后会直接打开招聘网站中的岗位页面。

## GitHub 一键同步

网页右侧“GitHub 同步”可以保存仓库地址和仅限该仓库的 Personal Access Token。“一键上传配置”会先保存当前页面，再将默认配置和全部分身配置写入 GitHub Actions Secret；“一键下载职位数据”会下载最近一次成功云端运行生成的数据库和缓存。

下载前必须停止本地监测。程序会校验 ZIP 路径、SQLite 完整性和 JSON 格式，并在覆盖前把本地旧数据备份到 `backups/github-sync-时间/`。Token 保存在本地 `github_sync.yaml` 中，该文件不会被 Git 提交，但仍应作为敏感文件妥善保管。

## 使用边界

招聘网站可能启用登录、验证码、访问频率控制或随时调整页面结构。请使用合理的监测间隔，遵守网站条款，不要尝试绕过验证码或访问控制。

拼多多招聘目前会在部分网络或自动化浏览器环境中拒绝职位搜索请求。程序检测到这种情况后会返回空结果并写入警告日志，不会把页面上的默认“热招岗位”误报为用户关键词结果；网站恢复正常响应后，无需修改配置即可继续抓取。

配置文件包含邮箱授权码和 API Key 时应仅保存在可信设备。若密钥曾上传到公开位置，需要在对应服务商后台撤销并重新生成。
