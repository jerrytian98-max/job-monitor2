# This file is safe to commit. On first local start it is copied to config.yaml.
# Edit the generated config.yaml in the Web UI; do not put personal values here.

job_sites: []
job_site_labels: {}
# Per-URL mode: search = use the global job keywords; fixed = capture only
# the jobs returned by that exact URL, without submitting global keywords.
job_site_modes: {}
job_keywords: []
cities: []
exclude_keywords: []
check_interval: 4

email:
  sender: ''
  auth_code: ''
  receiver: ''
  smtp_server: smtp.163.com
  smtp_port: 465

gemini_api_key: ''
gemini_model: gemini-3.5-flash-lite
ai_filter_prompt: ''
